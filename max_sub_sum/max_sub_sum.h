#pragma once
#include <chrono>
#include <iterator>
#include <algorithm>

template <typename Iter>

int max_sub_sum(Iter start, Iter end)
{
    return 0;
}
template <typename Iter>
int brute_max_sum(Iter start, Iter end)
{
    return 0;
}
